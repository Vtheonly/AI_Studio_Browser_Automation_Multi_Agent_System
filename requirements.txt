# Unified requirements for both codebases — install once, use everywhere
# Install: pip install -r requirements.txt
# Then:   playwright install chromium

# --- Browser automation (both samples) ---
playwright>=1.50

# --- Web server / API (sample 2) ---
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.0

# --- HTTP client (sample 2) ---
httpx>=0.27